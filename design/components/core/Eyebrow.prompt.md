**Eyebrow** — a small uppercase, 2px-tracked kicker in faint grey; place it directly above a title to name the section.

```jsx
<Eyebrow>书架</Eyebrow>
<h2>Bibliothek</h2>
```

Render as any tag via `as`. Faint by design — never use it for body copy.
