# 设计侧交接 · Mitlesen(回 施工反馈 2026-07)

> 作者=做设计的 Claude。回 `docs/DESIGN-FEEDBACK-2026-07.md` 的「§六 待回的裁决」。
> 三条裁决已定,并各自落成可看/可施工的稿。本文是**索引 + 施工下一步清单**。

---

## 一、三条裁决(§六 逐条回)

| # | 问题 | 裁决 | 依据 |
| --- | --- | --- | --- |
| §二-1 | 纸翻不翻 | **改 `colors.css`,readme 不动** | 第 4 公理是地基;手机 kit 早已纸/主题分开 |
| §二-2 | 第二 tab = 信箱 / 统计 | **信箱**(v1 只接本地卡片信箱) | readme + `.letters` CSS + 第 5 公理;统计在「我」里已重复 |
| §三 | 48 章长目录怎么退化 | **分区独立内滚 + 当前章开窗 + 手机十章分组** | 纯设计缺口,给出交互 spec |

一句话:**都不再阻塞手机批。** §二-2 靠「v1 只接现成后端」把工作量拉回与统计同级,贵的跨服务部分后置 v2。

---

## 二、交付物地图

| 交付物 | 位置 | 形态 | 状态 |
| --- | --- | --- | --- |
| §三 长目录退化交互 spec | `长目录退化.dc.html` → **1a** | 可交互 DC(真 DS 组件) | ✅ |
| §二-2 信箱 tab 稿 | 同上 → **2a** | 手机稿(火漆→破封→卡面) | ✅ |
| §二-1 换纸证明(3纸×2界矩阵) | 同上 → **2b** | 视觉证明 + token diff | ✅ |
| 桌面选纸器稿 | 同上 → **3a** | 顶栏「纸」控件 + 实时预览 | ✅ |
| `colors.css` 整份改好 | `patch-colors/colors.css` | 可覆盖替换 | ✅ |
| colors 重放说明 | `patch-colors/PATCH.md` | 补丁清单 | ✅ |
| 信箱 v1 接线/时序清单 | `patch-inbox/SPEC.md` | 接口 + 状态机 + 时序 | ✅ |

> `长目录退化.dc.html` 是 canvas 板,可自由缩放平移;右侧 Tweaks 可调「当前 ±N / 每组章数」看退化临界。

---

## 三、施工下一步(按批)

### 桌面批
- [ ] **长目录退化**:侧栏拆成「书架(有界内滚)/ 目录(独立内滚 + 当前章开窗)」两区;折叠条走冰蓝虚线;当前章滚出时浮「回到在读」。参照 **1a**。
- [ ] **选纸器**:顶栏加「纸」控件,写 `documentElement.dataset.paper`;默认 warm,暗色 chrome 默认 night;共用 `localStorage("mitlesen-paper")`。参照 **3a**。
- [ ] **colors.css**:用 `patch-colors/colors.css` 覆盖 `design/tokens/colors.css`,并同步部署侧 `_ds/.../tokens/colors.css`。见 `patch-colors/PATCH.md`。

### 手机批(裁决已定,可动)
- [ ] **底 tab** `统计→信箱`;`统计`(山脉/日历)移入「我」。
- [ ] **信箱 v1**:接 `card-inbox` / `image.svg` / `dismiss` / `card-collection`,三态状态机 sealed→opened→dismissed。**零后端改动**。全流程见 `patch-inbox/SPEC.md`,视觉见 **2a**。
- [ ] **长目录**:TOC sheet 限高内滚 + 十章手风琴分组,仅当前组展开。参照 **1a** 右侧手机帧。

---

## 四、回给施工侧的两个待确认
1. **信箱字段名**:`patch-inbox/SPEC.md` §5 按假设字段接;跑通后请回填 `card-inbox` 真实字段名(卡面走 `image.svg`,字段缺失不阻塞破封)。
2. **桌面选纸器上不上这轮**:线上 `reader.html` 现无选纸器。3a 已给稿;若这轮先不做,`colors.css` 补丁仍可先落(默认 warm,行为不变),选纸器随后补。

---

## 五、未触碰(明确不改)
- 桌面既有几何/色板/信纸物性:一律不动(反推现状收益有限,已按现状保持)。
- readme 五公理:不动——本轮所有裁决都是**贴着公理**做的(纸不反色=公理4;信箱破火漆=公理5;情绪仍只用蜡点=公理3)。
- 跨服务 Vorklang 慢递:后置 v2,不在本轮。
